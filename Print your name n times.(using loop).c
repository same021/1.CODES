#include <stdio.h>
int main() {
    int n;
    printf("Enter the value of n: ");
    scanf("%d", &n);

    // Print the name "sameer" n times
    for(int i = 0; i < n; i++) {
        printf("sameer\n");
    }

    return 0;
}
